# Container Census - Environment Variables for All Services
# Copy this file to .env and customize for your deployment

# ==============================================================================
# Required Configuration
# ==============================================================================

# Docker socket GID (required for server and agent)
# Auto-detect with: stat -c '%g' /var/run/docker.sock
# This must match the GID of the Docker socket on your host
DOCKER_GID=999

# ==============================================================================
# Server Configuration
# ==============================================================================

# Timezone for server logs and timestamps
TZ=UTC

# Optional: Custom config file path
# CONFIG_PATH=/app/config/config.yaml

# ==============================================================================
# Agent Configuration
# ==============================================================================

# Agent API Token
# Leave empty for auto-generation (check logs with: docker logs census-agent)
# Or set a secure token:
# AGENT_API_TOKEN=$(openssl rand -hex 32)
AGENT_API_TOKEN=

# Agent port (default: 9876)
AGENT_PORT=9876

# ==============================================================================
# Telemetry Collector Configuration
# ==============================================================================

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-to-a-secure-password

# Telemetry collector port (default: 8081)
TELEMETRY_PORT=8081

# Telemetry API Key (optional - for authentication)
# If set, clients must provide this key when submitting telemetry
# Generate with: openssl rand -hex 32
TELEMETRY_API_KEY=